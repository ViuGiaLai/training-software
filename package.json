{
  "name": "training-system",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "start": "concurrently \"mvn spring-boot:run\" \"browser-sync start --config bs-config.js\"",
    "dev": "concurrently \"mvn spring-boot:run\" \"browser-sync start --config bs-config.js\""
  },
  "dependencies": {
    "browser-sync": "^3.0.0"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
